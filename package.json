{
  "scripts": {
    "build": "npm install && tsc",
    "start": "npm install && tsc && pm2 start ecosystem.config.cjs",
    "stop": "pm2 stop BinanceApp",
    "restart": "pm2 restart BinanceApp",
    "reload": "npm run build && pm2 reload BinanceApp",
    "logs": "pm2 logs BinanceApp",
    "delete": "pm2 delete BinanceApp",
    "start:watch": "node dist/trade/market_watch/index.js"
  },
  "dependencies": {
    "@binance/connector": "^3.4.0",
    "@binance/connector-typescript": "^0.3.9",
    "@binance/spot": "^17.0.0",
    "lodash": "^4.17.21"
  },
  "type": "module",
  "devDependencies": {
    "@types/lodash": "^4.17.6",
    "@types/node": "^20.14.10"
  }
}
